@model IEnumerable<Models.Funder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.funderName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.funderName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.funderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.funderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.funderId })
        </td>
    </tr>
}

</table>
